import { HttpStatus, Module } from '@nestjs/common';
<%_ data.forEach((table) => {_%>
import { <%= table.entities.Entity %>Module } from './<%= table.entities.Entity %>/<%= table.entities.Entity %>.module';   
<%})%>


@Module({
  imports: [<%_ for (var i=0; i < data.length; i++) { _%><%= data[i].entities.Entity %>Module<%_ if (i !== data.length-1) { _%>,<% }} %>],
  controllers:[],
})
export class AppModule {}
