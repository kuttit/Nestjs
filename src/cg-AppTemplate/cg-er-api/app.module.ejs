import { Module } from '@nestjs/common';
<%_ data.forEach((table) => {_%>
import { <%= table.Entity %>Module } from './<%= table.Entity %>/<%= table.Entity %>.module';   
<%})%>

@Module({
  imports: [<%_ for (var i=0; i < data.length; i++) { _%><%= data[i].Entity %>Module<%_ if (i !== data.length-1) { _%>,<% }} %>],
  controllers:[],
})
export class AppModule {}
